
  it("should return 400 for title exceeding maximum length", async () => {
    const longTitle = "a".repeat(101); // 101 characters, exceeds max of 100
    const request = new Request("http://localhost/api/notes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: longTitle, content: "Test content" }),
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(400);
    expect(data.errors).toHaveLength(1);
    expect(data.errors[0].path).toBe("title");
    expect(data.errors[0].message).toContain("100 characters");
  });

  it("should return 400 for content exceeding maximum length", async () => {
    const longContent = "a".repeat(10001); // 10001 characters, exceeds max of 10000
    const request = new Request("http://localhost/api/notes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "Test Note", content: longContent }),
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(400);
    expect(data.errors).toHaveLength(1);
    expect(data.errors[0].path).toBe("content");
    expect(data.errors[0].message).toContain("10,000 characters");
  });

  it("should return 400 for empty title", async () => {
    const request = new Request("http://localhost/api/notes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "", content: "Test content" }),
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(400);
    expect(data.errors).toHaveLength(1);
    expect(data.errors[0].path).toBe("title");
    expect(data.errors[0].message).toContain("required");
  });

  it("should return 400 for empty content", async () => {
    const request = new Request("http://localhost/api/notes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "Test Note", content: "" }),
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(400);
    expect(data.errors).toHaveLength(1);
    expect(data.errors[0].path).toBe("content");
    expect(data.errors[0].message).toContain("required");
  });
